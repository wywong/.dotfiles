CC = gcc
CFLAGS = -c -g -Wall
LDFLAGS =
SOURCES =
OBJECTS = $(SOURCES:.c=.o)
EXECUTABLE =

all: $(SOURCES) $(EXECUTABLE)

$(EXECUTABLE): $(OBJECTS)
		$(CC) $(LDFLAGS) $(OBJECTS) -o $@

clean:
		rm -f *.o $(EXECUTABLE)

run:
		./

.c.o:
		$(CC) $(CFLAGS) $< -o $@
